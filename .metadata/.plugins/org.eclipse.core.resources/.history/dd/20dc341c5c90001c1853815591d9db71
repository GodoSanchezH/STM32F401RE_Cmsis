/*
 * main.c
 *
 *  Created on: Feb 16, 2022
 *      Author: Godo Sanchez
 */
#include "main.h"
#include "Clock_Config.h"


void GPIO_Init(void);

int main(void){

Init_Clock();

for(;;){

	}

	return 0;
}



void GPIO_Init(void){

	  RCC->AHB1ENR |= (1 << 0);						//	1. Habilitamos el clock GPIOA
	  GPIOA->MODER |=  (1 << 10);			//	2. Configuración del PIN A5 como salida
	  GPIOA->MODER &= ~(1 << 11);
	  //	3. Configuración del modo de salida
	  GPIOA->OTYPER &= ~(1 << 5);								//	0: Output push-pull (reset state)
	  //	4. Configuración de la velocidad del pin
	  GPIOA->OSPEEDR |= (1 << 10) | (1 << 11);					//	11: 100 MHz High speed
	  //	5. Configuración de resistencias pull up/ down
	  GPIOA->PUPDR &= ~(1 << 10) & ~(1 << 11);					//	00: No pull-up, pull-down



}


